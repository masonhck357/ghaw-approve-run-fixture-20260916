name: Held PR test

on:
  pull_request:

permissions:
  contents: read

jobs:
  marker:
    runs-on: ubuntu-latest
    steps:
      - name: Print inert historical marker
        run: printf '%s\n' 'GHAW_STALE_APPROVAL_A_20260916_7C4E13A2'

# Cross-fork association refresh.
